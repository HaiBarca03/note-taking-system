# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type NoteShare {
  id: Int!
  noteId: Int!
  note: Note!
  recipientId: Int
  shareToken: String!
  permission: SharePermission!
  expiredAt: DateTime
}

enum SharePermission {
  READ
  EDIT
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type GroupShare {
  id: Int!
  groupId: Int!
  group: Group!
  recipientId: Int
  shareToken: String!
  permission: SharePermission!
  expiredAt: DateTime
}

type Group {
  id: Int!
  name: String!
  userId: Int!
  isDeleted: Boolean!
  user: User!
  notes: [Note!]
  shares: [GroupShare!]
}

type Note {
  id: Int!
  title: String!
  content: String
  isDeleted: Boolean!
  deletedAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
  userId: Int!
  user: User!
  groupId: Int
  group: Group
  shares: [NoteShare!]
}

type User {
  id: Int!
  email: String!
  createdAt: DateTime!
  notes: [Note!]
  groups: [Group!]
  sharedNotes: [NoteShare!]
  sharedGroups: [GroupShare!]
}

type AuthResponse {
  access_token: String!
}

type Query {
  hello: String!
  notes: [Note!]!
  notesByGroup(groupId: Int!): [Note!]!
  note(id: Int!): Note!
  sharedNote(token: String!): Note!
  groups(includeDeleted: Boolean! = false): [Group!]!
  sharedGroupByToken(token: String!): Group!
  sharedNoteInGroup(token: String!, noteId: Int!): Note!
  groupSharePermission(token: String!): SharePermission!
}

type Mutation {
  register(data: RegisterInput!): AuthResponse!
  login(data: AuthInput!): AuthResponse!
  createNote(createNoteInput: CreateNoteInput!): Note!
  updateNote(updateNoteInput: UpdateNoteInput!): Note!
  removeNote(id: Int!): Boolean!
  shareNote(id: Int!, permission: SharePermission!): String!
  updateSharedNote(token: String!, content: String!): Note!
  createGroup(createGroupInput: CreateGroupInput!): Group!
  updateGroup(updateGroupInput: UpdateGroupInput!): Group!
  removeGroup(id: Int!): Boolean!
  shareGroup(id: Int!, permission: SharePermission!): String!
  updateNoteInSharedGroup(token: String!, noteId: Int!, content: String!): Note!
}

input RegisterInput {
  email: String!
  password: String!
  fullName: String
}

input AuthInput {
  email: String!
  password: String!
}

input CreateNoteInput {
  title: String!
  content: String
  groupId: Int
}

input UpdateNoteInput {
  id: Int!
  title: String
  content: String
  groupId: Int
  isDeleted: Boolean
}

input CreateGroupInput {
  name: String!
}

input UpdateGroupInput {
  id: Int!
  name: String
  isDeleted: Boolean
}